White paper card floating on the cream ground. Soft navy-tinted shadow, 20px radius, no border.

```jsx
<Card interactive>…</Card>
<Card tone="navy">şok ekranı içeriği</Card>
```

`tone`: `paper` (white), `sunken` (cream well), `navy` (dark shock surface). `interactive` lifts on hover.
