Big-emoji category tile for the picker grid; lifts on hover, rings coral when selected.

```jsx
<CategoryTile emoji="⛽" label="Benzin" selected onClick={pick} />
```

Lay out in a responsive grid (3-col mobile).
