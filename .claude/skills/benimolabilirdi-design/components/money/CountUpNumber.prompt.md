The shock mechanism — counts up from 0 to `value` on mount, tabular-nums, Turkish ₺ format.

```jsx
<CountUpNumber value={340.5} color="var(--coral-500)" size="var(--text-6xl)" />
```

Big + coral for the tax figure. Set `animate={false}` for static contexts (e.g. ShareCard export).
