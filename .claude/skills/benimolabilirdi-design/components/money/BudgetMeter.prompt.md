Depleting budget meter — the "spend the tax money" wallet metaphor, pinned atop the dream loop.

```jsx
<BudgetMeter total={340} remaining={120} />
```

Bar shrinks as `remaining` drops and turns coral below 20%.
